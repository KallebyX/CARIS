import { OfflinePage } from '@/components/offline-fallback'

export const metadata = {
  title: 'Offline - CÁRIS',
  description: 'Você está offline'
}

export default function Offline() {
  return <OfflinePage />
}
